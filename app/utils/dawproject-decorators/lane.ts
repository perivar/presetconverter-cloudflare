import { Referenceable } from "./referenceable";

/** Abstract base class for timeline lanes. */
export abstract class Lane extends Referenceable {
  // Lane does not have any additional properties in Java
}
