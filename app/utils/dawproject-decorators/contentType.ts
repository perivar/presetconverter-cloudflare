/** Represents the content type of a track or timeline. */
export enum ContentType {
  audio = "audio",
  automation = "automation",
  notes = "notes",
  video = "video",
  markers = "markers",
  tracks = "tracks",
}
